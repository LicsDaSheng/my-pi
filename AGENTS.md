# Overview

my-pi 是个人的 pi 配置源码,但 **不等同于** pi 就是 my-pi

my-pi 中的内容需要生效，必须要在 pi 中执行 pi update --extensions 更新 my-pi 的最新内容后才可以

## Constitution
- 所有过程内容(设计方案，spec，plan...)默认存放位置为.pi
- handbook中的内容是用户速查，操作手册，而不是过程材料聚集地